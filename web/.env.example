# ─── Backend API URL ─────────────────────────────────────────────────────────
# Point this to your backend service's public domain + /api/v1
# In Railway: set this to https://<backend-service-domain>/api/v1
# In Railway template variables: ${{backend.RAILWAY_PUBLIC_DOMAIN}}/api/v1
VITE_API_URL=http://localhost:8000/api/v1
